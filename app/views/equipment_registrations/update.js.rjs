page[@equipment_registration].replace :partial => 'equipment_registration'
page[@equipment_registration].visual_effect :scroll_to, :offset => -10
page['status_' + @equipment_registration.id.to_s].replace_html 'Equipment registration updated successfully!'
page['status_' + @equipment_registration.id.to_s].visual_effect :appear
page.delay(10.0) do
  page['status_' + @equipment_registration.id.to_s].visual_effect :fade
end