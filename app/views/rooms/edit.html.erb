<div id='page_title'>
  <%= title("Editing #{@room.name}", :h1) %>
</div>

<% form_for(@room) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update', {:class => 'btn btn-primary'} %> or <%= link_to 'Cancel', rooms_path %>
  </p>
<% end %>